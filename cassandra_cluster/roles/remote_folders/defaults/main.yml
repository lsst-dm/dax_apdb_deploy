#SPDX-License-Identifier: GPL-3.0-or-later
---
# defaults file for roles/remote_folders

# Default location for deployment tools.
deploy_folder: "{{ ansible_env.HOME }}/apdb_deploy"

# Location for all docker-compose things.
deploy_docker_folder: "{{ deploy_folder }}/docker"
